'use client';
export default function Index() {
  return (
    <button
      onClick={() => {
        window.location.href = 'https://aboutputri.netlify.app/ekyc-complete';
      }}
    >      redirect to
    </button>
  )
}